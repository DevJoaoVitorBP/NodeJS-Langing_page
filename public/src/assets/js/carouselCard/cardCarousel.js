// Initiate slick carousel for cards
$(".cards-slick-container").slick({
  dots: true, // Show dots
  infinite: false, // Disable infinite scroll
  speed: 700, // Set the speed of the carousel
  slidesToShow: 1, // Show one slide at a time
  centerMode: true, // Center the slides
  variableWidth: true, // Allow the slides to have variable width
});
